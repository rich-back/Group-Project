import React from "react";
import TimeLine from "../components/TimeLine";

const TimeLinePage = ({allElements}) => (
    <div>
    <TimeLine elements={allElements} />
    </div>
);

export default TimeLinePage;